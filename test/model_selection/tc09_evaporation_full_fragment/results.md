# TC09 Evaporation Full Fragment

Проверяет крупный реальный фрагмент из первой установки.

| Модель | Целевой язык | Запуск | Результат | Ошибки | Примечание |
|---|---|---:|---|---|---|
| openai/gpt-4o-mini | IL | 1 | неверно | пропущен оператор LD; неверная логика условия; сломан формат IL | В конце сломаны присваивания клапанов: `ST EVlv/ST SVlv/ST RVlv` идут без `LD SBEVlv/SbStg/RsStg`. |
| openai/gpt-4o-mini | IL | 2 | неверно | потеряна переменная; пропущен оператор LD; неверная логика условия; сломан формат IL | В конце появились `ST SBEVlv`, `ST Svlv`, `ST RsStg` вместо корректных присваиваний `EVlv/SVlv/RVlv`. |
| google/gemini-2.5-flash | IL | 1 | частично верно | сломан формат IL | Основная структура сохранена, но для отрицания внутри скобок используется `OR NOT` вместо IL-оператора `ORN`. |
| google/gemini-2.5-flash | IL | 2 | частично верно | сломан формат IL | Основная структура сохранена, но для отрицания внутри скобок используется `OR NOT` вместо IL-оператора `ORN`. |
| z-ai/glm-4.5-air:free | IL | 1 | неверно | неверная логика условия | В reset-условии `Htr` потеряно отрицание `SysOn`: используется `SysOn OR UTS ORN HtStg`. |
| z-ai/glm-4.5-air:free | IL | 2 | неверно | неверная логика условия; сломан формат IL | В reset-ветках использованы неверные начальные условия, а в `SbStg` внешний `NOT` записан как `AND`, а не `ANDN`. |
| deepseek/deepseek-v3.2 | IL | 1 | частично верно | сломан формат IL | Логика в целом сохранена, но часть скобок и `NOT` записаны не в эталонной IL-форме. |
| deepseek/deepseek-v3.2 | IL | 2 | частично верно | сломан формат IL | Логика в целом сохранена, но часть скобок и `NOT` записаны не в эталонной IL-форме. |
| qwen/qwen3.6-flash | IL | 1 | неверно | не выполнена трансляция | Вместо полного IL выведен только первый исходный ST-блок. |
| qwen/qwen3.6-flash | IL | 2 | частично верно | сломан формат IL | Основная структура сохранена, но для отрицания внутри скобок используется `OR NOT` вместо IL-оператора `ORN`. |
| openai/gpt-4o-mini | LD | 1 | неверно | сломан формат export; отсутствует _LD_ASSIGN; потеряна часть сетей | Заголовок указывает 12 сетей, фактически сформировано 16 выходов вместо 21; большая часть сетей без `_LD_ASSIGN`. |
| openai/gpt-4o-mini | LD | 2 | неверно | сломан формат export; отсутствует _LD_ASSIGN; потеряна часть сетей | Заголовок указывает 12 сетей, фактически сформировано 20 выходов вместо 21; во всех сетях отсутствует `_LD_ASSIGN`. |
| google/gemini-2.5-flash | LD | 1 | неверно | неверная логика условия; неверная полярность NOT; сломан формат export | В сети `SbStg` неверно инвертировано вложенное выражение `TP3 OR NOT TS`; заголовок указывает 20 сетей вместо 21. |
| google/gemini-2.5-flash | LD | 2 | неверно | неверная логика условия; неверная полярность NOT; сломан формат export | В сети `SbStg` неверно инвертировано вложенное выражение `TP3 OR NOT TS`; заголовок указывает 20 сетей вместо 21. |
| z-ai/glm-4.5-air:free | LD | 1 | неверно | неверная логика условия; неверная полярность NOT | В сети `SbStg` неверно раскрыт внешний `NOT`, полярности вложенных контактов инвертированы. |
| z-ai/glm-4.5-air:free | LD | 2 | неверно | неверная логика условия; неверная полярность NOT | В сети `SbStg` неверно раскрыт внешний `NOT`, полярности вложенных контактов инвертированы. |
| deepseek/deepseek-v3.2 | LD | 1 | неверно | неверная логика условия; неверная полярность NOT; сломан формат export | В `Fin` добавлен лишний контакт `1`, в `SbStg` неверно раскрыт `NOT`, заголовок указывает 20 сетей вместо 21. |
| deepseek/deepseek-v3.2 | LD | 2 | неверно | неверная логика условия; неверная полярность NOT; сломан формат export | В `SbStg` неверно раскрыт `NOT`, а заголовки `_NETWORKS/_LD_OPERATOR` содержат `-1`. |
| qwen/qwen3.6-flash | LD | 1 | частично верно | сломан формат export | 21 сеть и выходы сохранены, но во многих контактах сломан формат `_EXPRESSION`, из-за чего export не совпадает с эталоном. |
| qwen/qwen3.6-flash | LD | 2 | частично верно | сломан формат export | 21 сеть, контакты, полярности и выходы сохранены, но export-структура не совпадает с эталоном. |


