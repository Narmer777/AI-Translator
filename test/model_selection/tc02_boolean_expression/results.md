# TC02 Boolean Expression

Проверяет прямое присваивание со скобками, `AND`, `OR`.

| Модель | Целевой язык | Запуск | Результат | Ошибки | Примечание |
|---|---|---:|---|---|---|
| openai/gpt-4o-mini | IL | 1 | неверно | неверная логика условия | Модель изменила группировку выражения: `SysOn OR (TP3 AND SBSink)` вместо `(SysOn AND TP3) OR SBSink`. |
| openai/gpt-4o-mini | IL | 2 | неверно | неверная логика условия; сломан формат IL | Модель выдала лишний незавершенный фрагмент IL после `ST Pmp`. |
| google/gemini-2.5-flash | IL | 1 | частично верно | сломан формат IL | Логика сохранена, но скобки записаны не в эталонном формате. |
| google/gemini-2.5-flash | IL | 2 | частично верно | сломан формат IL | Логика сохранена, но скобки записаны не в эталонном формате. |
| z-ai/glm-4.5-air:free | IL | 1 | верно |  | Совпадает с эталоном. |
| z-ai/glm-4.5-air:free | IL | 2 | частично верно | сломан формат IL | Логика сохранена, но добавлена лишняя открывающая скобка в строке `AND ( (SysOn`. |
| deepseek/deepseek-v3.2 | IL | 1 | верно |  | Совпадает с эталоном. |
| deepseek/deepseek-v3.2 | IL | 2 | неверно | сломан формат IL | В IL присутствует лишняя закрывающая скобка. |
| qwen/qwen3.6-flash | IL | 1 | неверно | сломан формат IL | Внутри скобочного выражения модель вставила лишний `LD SysOn`. |
| qwen/qwen3.6-flash | IL | 2 | частично верно | сломан формат IL | Логика сохранена, но скобки записаны не в эталонном формате. |
| openai/gpt-4o-mini | LD | 1 | частично верно | сломан формат export; нет закрытия выражения | Логика сохранена, но отсутствуют закрывающие `_EXPRESSION _POSITIV` для вложенного выражения. |
| openai/gpt-4o-mini | LD | 2 | частично верно | сломан формат export; нет закрытия выражения | Логика сохранена, но отсутствуют закрывающие `_EXPRESSION _POSITIV` для вложенного выражения. |
| google/gemini-2.5-flash | LD | 1 | верно |  | Совпадает с эталоном. |
| google/gemini-2.5-flash | LD | 2 | верно |  | Совпадает с эталоном. |
| z-ai/glm-4.5-air:free | LD | 1 | верно |  | Совпадает с эталоном. |
| z-ai/glm-4.5-air:free | LD | 2 | верно |  | Совпадает с эталоном. |
| deepseek/deepseek-v3.2 | LD | 1 | частично верно | сломан формат export; нет закрытия выражения | Логика сохранена, но отсутствует закрытие вложенного `_LD_AND`. |
| deepseek/deepseek-v3.2 | LD | 2 | верно |  | Совпадает с эталоном. |
| qwen/qwen3.6-flash | LD | 1 | верно |  | Совпадает с эталоном. |
| qwen/qwen3.6-flash | LD | 2 | верно |  | Совпадает с эталоном. |


