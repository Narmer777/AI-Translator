# TC07 Modes Block

Проверяет несколько функциональных переменных, сложные скобки и вложенный `NOT`.

| Модель | Целевой язык | Запуск | Результат | Ошибки | Примечание |
|---|---|---:|---|---|---|
| openai/gpt-4o-mini | IL | 1 | частично верно | сломан формат IL | Для отрицания внутри скобок использовано `OR LDN TS` и `OR LDN LS0` вместо `ORN TS` и `ORN LS0`. |
| openai/gpt-4o-mini | IL | 2 | частично верно | сломан формат IL | Для отрицания внутри скобок использовано `OR LDN TS` и `OR LDN LS0` вместо `ORN TS` и `ORN LS0`. |
| google/gemini-2.5-flash | IL | 1 | частично верно | сломан формат IL | Для отрицания внутри скобок использовано `OR NOT TS` и `OR NOT LS0` вместо IL-оператора `ORN`. |
| google/gemini-2.5-flash | IL | 2 | частично верно | сломан формат IL | Для отрицания внутри скобок использовано `OR NOT TS` и `OR NOT LS0` вместо IL-оператора `ORN`. |
| z-ai/glm-4.5-air:free | IL | 1 | неверно | модель не вернула ответ | `finish_reason=length`, `content=null`. |
| z-ai/glm-4.5-air:free | IL | 2 | верно |  | Совпадает с эталоном. |
| deepseek/deepseek-v3.2 | IL | 1 | верно |  | Совпадает с эталоном. |
| deepseek/deepseek-v3.2 | IL | 2 | верно |  | Совпадает с эталоном. |
| qwen/qwen3.6-flash | IL | 1 | частично верно | сломан формат IL | Добавлена лишняя закрывающая скобка после первого выражения `Pmp`. |
| qwen/qwen3.6-flash | IL | 2 | частично верно | сломан формат IL | Добавлена лишняя закрывающая скобка после первого выражения `Pmp`. |
| openai/gpt-4o-mini | LD | 1 | неверно | неверная логика условия; сломан формат export | В нескольких сетях изменены полярности контактов и заголовок указывает 6 сетей вместо 5. |
| openai/gpt-4o-mini | LD | 2 | неверно | неверная логика условия; потеряна переменная; сломан формат export | Потерян `SBSink`, переменная `_Compl` записана как `Compl`, изменены полярности контактов. |
| google/gemini-2.5-flash | LD | 1 | частично верно | сломан формат export | Контакты, полярности и выходы сохранены, но структура операторов не совпадает с эталоном. |
| google/gemini-2.5-flash | LD | 2 | частично верно | сломан формат export | Контакты, полярности и выходы сохранены, но структура операторов не совпадает с эталоном. |
| z-ai/glm-4.5-air:free | LD | 1 | неверно | модель не вернула ответ | `finish_reason=length`, `content=null`. |
| z-ai/glm-4.5-air:free | LD | 2 | неверно | модель не вернула ответ | `finish_reason=length`, `content=null`. |
| deepseek/deepseek-v3.2 | LD | 1 | неверно | неверная логика условия; потеряна часть условия | В сети `SbStg` потеряна вложенная часть `LS0 AND _SVlv AND (TP3 OR NOT TS)`. |
| deepseek/deepseek-v3.2 | LD | 2 | неверно | неверная логика условия; потеряна часть условия | В сети `SbStg` потеряна вложенная часть `LS0 AND _SVlv AND (TP3 OR NOT TS)`. |
| qwen/qwen3.6-flash | LD | 1 | неверно | неверная логика условия; неверная полярность NOT | В сети `SbStg` неверно раскрыт внешний `NOT`, полярности вложенных контактов инвертированы. |
| qwen/qwen3.6-flash | LD | 2 | частично верно | сломан формат export | Контакты, полярности и выходы сохранены, но export-структура не совпадает с эталоном. |


