# TC10 Mixer Full Fragment

Проверяет крупный реальный фрагмент из второй установки.

| Модель | Целевой язык | Запуск | Результат | Ошибки | Примечание |
|---|---|---:|---|---|---|
| openai/gpt-4o-mini | IL | 1 | неверно | неверная логика условия; сломан формат IL | В `Fin` потеряна группировка `OR`, появляется некорректный фрагмент `OR ANDN TS2`; reset `Htr` записан как `AND (ANDN SysOn ...)`. |
| openai/gpt-4o-mini | IL | 2 | неверно | неверная логика условия; сломан формат IL | В `Fin` потеряна группировка, используется `S Fin` вместо `ST Fin`, присутствует некорректный фрагмент `OR ANDN TS2`. |
| google/gemini-2.5-flash | IL | 1 | частично верно | сломан формат IL | Основная структура сохранена, но отрицания внутри скобок записаны как `NOT TS1/NOT SysOn`, а не эталонной IL-формой. |
| google/gemini-2.5-flash | IL | 2 | частично верно | сломан формат IL | Основная структура сохранена, но отрицания внутри скобок записаны как `NOT TS1/NOT SysOn`, а не эталонной IL-формой. |
| z-ai/glm-4.5-air:free | IL | 1 | неверно | неверная логика условия; сломан формат IL | Reset-ветки начинаются с `LDN _HtrErr/_MtrErr/_SysOn/_Htr` вместо положительных предыдущих состояний. |
| z-ai/glm-4.5-air:free | IL | 2 | неверно | неверная логика условия; сломан формат IL | Reset-ветки начинаются с `LDN _HtrErr/_MtrErr/_SysOn/_Htr` вместо положительных предыдущих состояний. |
| deepseek/deepseek-v3.2 | IL | 1 | частично верно | сломан формат IL | Логика в целом сохранена, но часть скобок и отрицаний записаны не в эталонной IL-форме. |
| deepseek/deepseek-v3.2 | IL | 2 | частично верно | сломан формат IL | Логика в целом сохранена, но часть скобок и отрицаний записаны не в эталонной IL-форме. |
| qwen/qwen3.6-flash | IL | 1 | частично верно | сломан формат IL | Логика в целом сохранена, но часть скобок и отрицаний записаны не в эталонной IL-форме. |
| qwen/qwen3.6-flash | IL | 2 | неверно | неверная логика условия; сломан формат IL | Reset-ветки начинаются с `LDN _HtrErr/_MtrErr/_SysOn/_Htr`, из-за чего сброс состояний описан неверно. |
| openai/gpt-4o-mini | LD | 1 | неверно | сломан формат export; потеряна часть сетей; неверная логика условия | Заголовок указывает 12 сетей вместо 21, фактически сформировано 17 выходов, часть условий и выходов перепутана. |
| openai/gpt-4o-mini | LD | 2 | неверно | сломан формат export; потеряна часть сетей; неверная логика условия | Заголовок указывает 12 сетей вместо 21, фактически сформировано 16 выходов, сети смещены и часть выходов потеряна. |
| google/gemini-2.5-flash | LD | 1 | частично верно | сломан формат export; неверное количество LD-сетей | 21 выход сохранён, но заголовок указывает 25 сетей вместо 21 и export-структура не совпадает с эталоном. |
| google/gemini-2.5-flash | LD | 2 | частично верно | сломан формат export; неверное количество LD-сетей | 21 выход сохранён, но заголовок указывает 25 сетей вместо 21 и export-структура не совпадает с эталоном. |
| z-ai/glm-4.5-air:free | LD | 1 | частично верно | сломан формат export; нет закрытия выражения | 21 сеть и выходы сохранены, но не закрыта часть `_EXPRESSION`. |
| z-ai/glm-4.5-air:free | LD | 2 | частично верно | сломан формат export; нет закрытия выражения | 21 сеть и выходы сохранены, но не закрыта часть `_EXPRESSION`. |
| deepseek/deepseek-v3.2 | LD | 1 | частично верно | сломан формат export; неверное количество LD-сетей | Фактически есть 21 выход, но заголовок указывает 20 сетей. |
| deepseek/deepseek-v3.2 | LD | 2 | частично верно | сломан формат export; неверное количество LD-сетей | Фактически есть 21 выход, но заголовок указывает 24 сети. |
| qwen/qwen3.6-flash | LD | 1 | частично верно | сломан формат export; нет закрытия выражения | 21 сеть и выходы сохранены, но export-структура не совпадает с эталоном. |
| qwen/qwen3.6-flash | LD | 2 | частично верно | сломан формат export; нет закрытия выражения | 21 сеть и выходы сохранены, но во многих контактах нарушен формат `_EXPRESSION`. |


